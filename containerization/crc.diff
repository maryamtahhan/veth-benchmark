diff --git a/usrtools/txgen/app/txgen.c b/usrtools/txgen/app/txgen.c
index 71021bf..53038a9 100644
--- a/usrtools/txgen/app/txgen.c
+++ b/usrtools/txgen/app/txgen.c
@@ -36,7 +36,7 @@
 #include "seq.h"                 // for pkt_seq_t
 #include "stats.h"               // for pkt_stats_t, txgen_page_stats
 #include "latency.h"
-
+#include "cne_net_crc.h"
 /* Allocated the txgen structure for global use */
 txgen_t txgen;
 
@@ -559,6 +559,15 @@ txgen_send_pkts(port_info_t *info)
         } else {
             xb->data_len = info->pkt.pktSize;
             memcpy(pktmbuf_mtod(xb, uint8_t *), (uint8_t *)&info->pkt.hdr, xb->data_len);
+            uint32_t crc = cne_net_crc_calc(pktmbuf_mtod(xb, uint8_t *), xb->data_len, CNE_NET_CRC32_ETH);
+            memcpy(pktmbuf_mtod(xb, uint8_t *)+xb->data_len, (uint8_t *)&crc, sizeof(uint32_t));
+            xb->data_len += sizeof(uint32_t);
+#if 0
+            uint32_t csum = cksum_update(pktmbuf_mtod(xb, uint8_t *), xb->data_len, 0);
+            uint16_t final_csum = cksumDone(csum);
+            memcpy(pktmbuf_mtod(xb, uint8_t *)+xb->data_len, (uint8_t *)&final_csum, sizeof(uint16_t));
+            xb->data_len += sizeof(uint16_t);
+#endif
         }
     }
     info->tx_mbufs.len += nb;
diff --git a/usrtools/txgen/app/udp.c b/usrtools/txgen/app/udp.c
index 69247b7..77ad475 100644
--- a/usrtools/txgen/app/udp.c
+++ b/usrtools/txgen/app/udp.c
@@ -44,9 +44,9 @@ txgen_udp_hdr_ctor(pkt_seq_t *pkt, void *hdr, int type __cne_unused)
     udp->dst_port  = htons(pkt->dport);
 
     udp->dgram_cksum = 0;
-    udp->dgram_cksum = cne_ipv4_udptcp_cksum(ipv4, (const void *)udp);
-    if (udp->dgram_cksum == 0)
-        udp->dgram_cksum = 0xFFFF;
+    //udp->dgram_cksum = cne_ipv4_udptcp_cksum(ipv4, (const void *)udp);
+    //if (udp->dgram_cksum == 0)
+    //    udp->dgram_cksum = 0xFFFF;
 
     /* Return the original pointer for IP ctor */
     return hdr;
